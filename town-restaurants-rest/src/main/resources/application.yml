spring:
  datasource:
    url: jdbc:mysql://localhost:3306/town_restaurants
    username: root
    password: yerevan21
  mail:
    host: smtp.gmail.com
    username:
    password:
    port: 587
    smtp:
      auth: true
      starttls:
        enable: true
server:
  servlet:
    context-path: /api/v1
    port: 8081
jwt:
  secret: k2y75xxx
  expiration: 330000

logging:
  logback:
    rollingpolicy:
      file-name-pattern: ${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz
      max-file-size: 10MB
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} %-5level - %msg%n"
  level:
    root: info
    am.itspace: info

#  liquibase:
#    change-log: classpath:db/changelog/migration.xml


#http://localhost:8081/api/v1/swagger-ui/index:
#  html: